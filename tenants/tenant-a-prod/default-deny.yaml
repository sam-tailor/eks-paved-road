apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: default-deny
  namespace: tenant-a-prod
spec:
  # By leaving the 'selector' empty, this policy applies to ALL pods in the namespace
  {}
  # By omitting the 'rules' section and not setting an 'action', it defaults to DENY
