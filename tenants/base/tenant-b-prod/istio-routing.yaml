apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: public-gateway
  namespace: tenant-b-prod
spec:
  selector:
    app: istio-ingress
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: web-route
  namespace: tenant-b-prod
spec:
  hosts:
  - "*"
  gateways:
  - public-gateway
  http:
  # NEW ROUTE: Intercept /app2 and route to web-server-2
  - match:
    - uri:
        prefix: /app2
    rewrite:
      uri: /
    route:
    - destination:
        host: web-server-2
        port:
          number: 80
  # EXISTING ROUTE: Intercept /tenant-b and route to web-server
  - match:
    - uri:
        prefix: /tenant-b
    rewrite:
      uri: /
    route:
    - destination:
        host: web-server
        port:
          number: 80
---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-public-web
  namespace: tenant-b-prod
spec:
  selector:
    matchLabels:
      app: web
  action: ALLOW
  rules:
  - to:
    - operation:
        ports: ["80"]
---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-public-web-2
  namespace: tenant-b-prod
spec:
  selector:
    matchLabels:
      app: web-server-2
  action: ALLOW
  rules:
  - to:
    - operation:
        ports: ["80"]
